<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="icon" type="image/x-icon" href="/images/Durga Puja Banner.jpg">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-gradient-to-b from-yellow-300 to-orange-200 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-8">
            <div class="flex items-center">
                <img src="/images/Durga Puja Banner.jpg" alt="Durgotsav Logo" class="h-12 w-auto mr-4">
                <h1 class="text-3xl font-bold">Durgotsav Voting Dashboard</h1>
            </div>
            <button id="logoutButton" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                Logout
            </button>
        </div>
        
        <div id="adminContent" class="hidden">
            <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
                <h2 class="text-xl font-semibold mb-4">Votes Data</h2>
                <div class="mb-4 flex items-center flex-wrap">
                    <input type="text" id="dateRange" placeholder="Select date range" class="border rounded px-2 py-1 mr-2 mb-2">
                    <input type="text" id="nameFilter" placeholder="Filter by name" class="border rounded px-2 py-1 mr-2 mb-2">
                    <input type="text" id="mobileFilter" placeholder="Filter by mobile" class="border rounded px-2 py-1 mr-2 mb-2">
                    <select id="criteriaFilter" class="border rounded px-2 py-1 mr-2 mb-2">
                        <option value="">All Criteria</option>
                        <!-- Criteria options will be dynamically populated here -->
                    </select>
                    <select id="pandalFilter" class="border rounded px-2 py-1 mr-2 mb-2">
                        <option value="">All Pandals</option>
                        <!-- Pandal options will be dynamically populated here -->
                    </select>
                    <button id="applyFilters" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-2 mb-2">
                        Apply Filters
                    </button>
                    <button id="exportExcel" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mb-2">
                        Export to Excel
                    </button>
                </div>
                <div class="mb-12">
                    <div id="voteSummary" class="grid grid-cols-1 gap-8">
                        <div class="bg-white rounded-lg shadow-lg p-6 hover:shadow-xl transition-shadow duration-300 border border-gray-200">
                            <h4 class="text-xl font-medium mb-4 text-center text-blue-600">Votes by Criteria</h4>
                            <div class="w-full h-72">
                                <canvas id="criteriaChart" class="w-full h-full"></canvas>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="overflow-x-auto">
                    <table id="votesTable" class="min-w-full bg-white">
                        <thead>
                            <tr>
                                <th class="py-2 px-4 border-b">Name</th>
                                <th class="py-2 px-4 border-b">Mobile Number</th>
                                <th class="py-2 px-4 border-b">Criteria</th>
                                <th class="py-2 px-4 border-b">Pandal Name</th>
                                <th class="py-2 px-4 border-b">Timestamp</th>
                            </tr>
                        </thead>
                        <tbody id="tableBody">
                            <!-- Table rows will be dynamically populated here -->
                        </tbody>
                    </table>
                </div>
                <div class="mt-4 flex justify-between items-center">
                    <div>
                        <button id="prevPage" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                            Previous
                        </button>
                        <button id="nextPage" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded ml-2">
                            Next
                        </button>
                        <span id="paginationInfo" class="ml-4"></span>
                    </div>
                </div>
            </div>
        </div>
        <div id="accessDenied" class="hidden">
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                <strong class="font-bold">Access Denied!</strong>
                <span class="block sm:inline"> You do not have admin privileges to view this content.</span>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.17.0/xlsx.full.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="/js/dashboard.js"></script>
</body>
</html>